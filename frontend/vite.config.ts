import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    proxy: {
      // Strip /api prefix and forward to FastAPI on 127.0.0.1 (explicit IPv4)
      // This avoids the Linux "localhost → ::1" IPv6 resolution issue
      '/api': {
        target: 'http://127.0.0.1:8000',
        changeOrigin: true,
        rewrite: (path) => path.replace(/^\/api/, ''),
      },
    },
  },
})
